# .travis.yml
language: node_js
node_js:
  - 12

jobs:
  include:
    - stage: test
      script: npm run test
    - stage: deployLibraryToNPM
      node_js: lts/*
      script: npm run build
      deploy:
        provider: script
        skip_cleanup: true
        script: npx semantic-release
    - stage: deployDemoSiteToGitHubPage
      script: npm run build-demo-site
      deploy:
        provider: pages
        skip_cleanup: true
        github_token: 1bd66a6e018565cb42c0f8c2a4d208d10cc15e93
        keep_history: true
        local_dir: demoSite
